{
  "name": "codemirror-in-30",
  "version": "1.0.0",
  "description": "Replace a textarea with CodeMirror in under 30 minutes and under 30 lines of code (demo)",
  "main": "main.js",
  "scripts": {
    "build": "esbuild --minify --bundle main.js --outfile=editor.js",
    "serve": "esbuild --minify --bundle main.js --outfile=editor.js --servedir=."
  },
  "author": "Nat Knight <nathaniel.ep@gmail.com>",
  "license": "MPL-2.0",
  "dependencies": {
    "@codemirror/lang-markdown": "^6.0.5",
    "codemirror": "^6.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.16.10"
  }
}
